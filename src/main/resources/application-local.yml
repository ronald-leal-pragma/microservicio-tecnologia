spring:
  sql:
    init:
      mode: always
  config:
    activate:
      on-profile: local
  r2dbc:
    url: r2dbc:mysql://localhost:3306/tecnologia_db?serverTimezone=America/Bogota
    username: user_tecno
    password: 18566621
    pool:
      enabled: true
      initial-size: 5
      max-size: 20
      max-idle-time: 30m
      validation-query: SELECT 1
server:
  port: 8081
